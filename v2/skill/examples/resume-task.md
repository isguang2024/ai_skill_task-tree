# 任务续跑示例

适用于中断后继续推进 `task-tree-v2` 的任务。

## 示例提示词

请按 `task-tree-v2` 技能约定继续执行当前任务。

要求：

- 先读取当前任务状态与上下文
- 明确下一可执行节点
- 不重复已经完成的改动
- 如果涉及接口改动，保持 HTTP 与 MCP 对齐
- 完成后执行必要验证

## 适用场景

- 跨会话续跑
- 中断后恢复
- 多阶段任务继续推进

## 预期输出

- 当前状态摘要
- 下一步执行项
- 实际改动结果
- 验证结果
