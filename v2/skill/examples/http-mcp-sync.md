# HTTP / MCP 对齐示例

适用于检查或补齐本项目中 HTTP API 与 MCP 工具的一致性。

## 示例提示词

请检查当前项目功能在 HTTP 和 MCP 两条入口上是否一致。

要求：

- 找出只有 HTTP 没有 MCP 的能力
- 找出只有 MCP 没有 HTTP 的能力
- 直接补齐缺口
- 同步更新相关文档或清单
- 完成后执行测试或构建验证

## 适用场景

- 新增能力后补 MCP
- 回归检查
- 发布前接口核对

## 预期输出

- 差异点列表
- 已补齐项
- 涉及文件
- 验证结果
